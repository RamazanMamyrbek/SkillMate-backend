spring:
  application:
    name: skillmate-backend
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: 210103040@stu.sdu.edu.kz
    password: kwlr ynsa fgib pyyt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true